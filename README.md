# Mark Rshq Bot (Webhook + Points System)

بوت تيليجرام للرشق (طلب يدوي) مع نظام نقاط واشتراك في القنوات + تحميل فيديوهات.

## ✅ المميزات
- حفظ المشتركين في `users.json`.
- نظام نقاط:
  - نقاط ترحيب عند أول دخول.
  - نقاط مقابل الاشتراك في قنوات محددة (channels.json) مع تحقق حقيقي `getChatMember`.
- طلبات رشق:
  - انستغرام متابعين.
  - تيك توك متابعين.
  - تيليجرام أعضاء مجموعات.
  - الطلبات تحفظ في `orders.json` وتنفذ يدوياً من الأدمن.
- تحميل الفيديوهات من روابط (انستغرام / تيك توك / يوتيوب...) باستخدام `yt-dlp`.
- نظام أدمن داخل البوت:
  - `/admin` لوحة أوامر.
  - `/stats` إحصائيات.
  - `/orders` عرض الطلبات.
  - `/broadcast` إرسال رسالة جماعية.
  - `/addpoints user_id amount`
  - `/setpoints user_id amount`

## 🌍 متغيرات البيئة في Railway (Variables)

يجب ضبط هذه المتغيرات في مشروع Railway:

| المتغير | الوصف |
|--------|--------|
| `TOKEN` | توكن بوت تيليجرام من BotFather |
| `ADMIN_ID` | آيدي الأدمن (int) |
| `WEBHOOK_URL` | رابط تطبيق Railway بدون `/` في النهاية, مثال: `https://your-app.up.railway.app` |
| `PORT` | يضبط تلقائياً من Railway (لا تحتاج تغييره عادة) |

> لا تضع التوكن داخل الكود في GitHub، اتركه فارغاً واستخدم المتغيرات.

## 🚀 طريقة الرفع على GitHub

```bash
git init
git add .
git commit -m "Initial Mark Rshq Bot"
git branch -M main
git remote add origin https://github.com/your-username/mark-rshq-bot.git
git push -u origin main
